<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Custom Task Manager" Height="500" Width="850"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            
            <TextBlock Text="Refresh Interval:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
            
            <ComboBox x:Name="IntervalComboBox" Width="90" SelectedIndex="1" VerticalAlignment="Center">
                <ComboBoxItem Content="1 sec"/>
                <ComboBoxItem Content="2 sec"/>
                <ComboBoxItem Content="5 sec"/>
            </ComboBox>

            <Button x:Name="BtnToggleTimer" Content="Pause" Width="75" Margin="12,0,0,0" Padding="6,3"/>

            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="15,0"/>

            <Button x:Name="BtnStartProcess" Content="Run New Task..." Width="130" Margin="0,0,12,0" Padding="6,3"/>

            <Button x:Name="BtnKillProcess" Content="End Task" Width="100" Foreground="Red" FontWeight="Bold" Padding="6,3"/>
            
        </StackPanel>

        <DataGrid Grid.Row="1" 
                  x:Name="ProcessGrid" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#E2E8F0">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Process Name" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="PID" Binding="{Binding Id}" Width="Auto" MinWidth="65"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="*"/>
                <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime}" Width="1.5*"/>
            </DataGrid.Columns>
            
        </DataGrid>
        
    </Grid>
</Window>